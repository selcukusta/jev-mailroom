# TypeSafe API key. Alternatively put the key in ~/.typesafe.key
TYPESAFE_API_KEY=

# Optional: pin the model instead of the moving jev-latest alias.
# TYPESAFE_DEFAULT_MODEL=jev-1.13.0

# Optional: verbose SDK logging (debug, info, warning, error, off)
# TYPESAFE_LOG_LEVEL=info
